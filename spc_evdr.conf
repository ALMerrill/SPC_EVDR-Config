CONFIG_ZMK_USB=y

# DIAGNOSTIC: combined with the composite-kscan split, add settle time in
# case the native column (8/9) reads need more margin after the fast GPIO
# edge before the row lines are sampled. Revert once tested.
CONFIG_ZMK_KSCAN_MATRIX_WAIT_BEFORE_INPUTS=500
CONFIG_ZMK_KSCAN_MATRIX_WAIT_BETWEEN_OUTPUTS=500
