CONFIG_ZMK_USB=y

# DIAGNOSTIC: display disabled to test whether OLED I2C traffic (shares the
# xiao_i2c bus with the PCA9555 column expander) is corrupting matrix scans
# on column 8 (O / L / . keys). Revert once tested.
CONFIG_ZMK_DISPLAY=n
